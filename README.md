# atlas

## Setup

1. Copy `backend/.env.example` to `backend/.env` and fill in your API keys (never commit `.env`).
2. Run `setup-backend.bat` (or install backend deps with `uv`), then start with `start-atlas.ps1` / `start-atlas.bat`.
